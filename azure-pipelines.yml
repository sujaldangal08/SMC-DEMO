trigger:
- Sujal

pool:
  vmImage: ubuntu-latest

variables:
  phpVersion: '8.3.6'

jobs:
- deployment: VMDeploy
  displayName: Web deploy
  environment:
    name: 'laravel'
    resourceType: VirtualMachine
    resourceName: 'laravel'
  strategy:
    runOnce:
      deploy:
        steps:
        - task: Bash@3
          displayName: 'Run deploy.sh script'
          inputs:
            filePath: '/home/laraveluser/SMC-DEMO/deploy.sh'
